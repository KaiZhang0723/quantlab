# Default configuration for the quantlab CLI.
#
# Currently consumed only by the ``fetch`` subcommand:
#     quantlab --config configs/default.yaml fetch --out reports/sample_prices.csv
#
# Other subcommands (backtest, forecast, var, sql) read all parameters from
# their CLI flags directly. Override or supply tickers / dates on the command
# line as needed.

tickers:
  - AAPL
  - MSFT
  - GOOGL
  - AMZN
  - NVDA
  - JPM
  - XOM
  - JNJ
  - PG
  - WMT

start: "2019-01-01"
end:   "2024-12-31"
